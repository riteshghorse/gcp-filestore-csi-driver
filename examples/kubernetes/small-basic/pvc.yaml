kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: basic-pvc
  namespace: gcp-filestore-csi-driver
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: small-basic-rwx
  resources:
    requests:
      storage: 1.2Ti
