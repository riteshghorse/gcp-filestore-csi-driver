apiVersion: v1
kind: Pod
metadata:
  name: inbuilt-app
spec:
  containers:
  - name: inbuilt-app
    image: nginx
    volumeMounts:
    - mountPath: /usr/share/nginx/html
      name: vol1
  volumes:
  - name: vol1
    persistentVolumeClaim:
      claimName: inbuilt-zonal
