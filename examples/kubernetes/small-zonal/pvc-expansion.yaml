apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: small-pvc
  namespace: gcp-filestore-csi-driver
spec:
  resources:
    requests:
      storage: 3Ti
  storageClassName: small-zonal-rwx
  accessModes:
  - ReadWriteMany
