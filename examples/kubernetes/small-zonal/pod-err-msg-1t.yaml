apiVersion: v1
kind: Pod
metadata:
  name: zonal-small-app-err
  namespace: gcp-filestore-csi-driver
spec:
  containers:
  - name: zonal-small-app-err
    image: nginx
    volumeMounts:
    - mountPath: /usr/share/nginx/html
      name: vol1
  volumes:
  - name: vol1
    persistentVolumeClaim:
      claimName: zonal-small
